# Quick Start

```python
import {{ cookiecutter.package_name }}

print({{ cookiecutter.package_name }}.__version__)
```

Replace this page with the shortest example that shows what
{{ cookiecutter.project_name }} is for: a handful of lines, real input, real
output, no setup beyond the [installation](installation.md).
